#include <iostream>
#include <deque>
using namespace std;

int main() {
	setlocale(LC_ALL, "ru");

	deque <int> double_queue; 
	/* Ёто двусторонн€€ очередь. ѕо ней можно так же итерироватьс€ как по массиву, но есть особенности. ¬ массив накладно добавл€ть новые элементы, а в списке получать к ним доступ
	deque - это гибрид списка и массива. [][][][] -> [][][][] -> [][][][]. ” него средн€€ скорость записи и обращени€ к элементам.

	ћетоды те же, что и массива.
	*/

	return 0;
}